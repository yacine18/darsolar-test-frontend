import type { NextPage } from "next";
import CompaniesList from "../components/CompaniesList";

const Home: NextPage = () => {
  return (
    <div>
      <CompaniesList />
    </div>
  );
};

export default Home;
